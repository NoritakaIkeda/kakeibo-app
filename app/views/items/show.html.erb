<h1>履歴</h1>
<a href="index.html">ホームに戻る</a>
<% @items.each do |item| %>
    <p><%= item.itemname %> 
    <%= item.typename %> 
    <%= item.price %>
    <%= item.date %>
    <%= item.memo %></p>
    
  <% end %>
